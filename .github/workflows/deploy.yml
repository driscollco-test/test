name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deployment:
    name: Deploy Service
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        working-directory: deploy
        run: make docker

      - name: Deploy Service
        working-directory: deploy
        run: make deploy
